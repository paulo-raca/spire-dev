version: 2
updates:
  # Both base images (alpine + ghcr.io/spiffe/spire-*) track upstream
  # :latest and are rebuilt daily by CI — nothing for dependabot's docker
  # ecosystem to do. Only github-actions pins remain managed here.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: daily
